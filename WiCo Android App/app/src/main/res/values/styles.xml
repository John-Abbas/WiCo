<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    <!-- Optional, on Android 5+ you can modify the colorPrimaryDark color to match the windowBackground color for further branding-->
    <!-- <item name="colorPrimaryDark">@android:color/white</item> -->
    </style>

    <style name="WiCo_Purple" parent="AppTheme" >
        <item name="android:colorBackground">@color/backGround_Purple</item>
        <item name="android:statusBarColor">@color/statusBar_Purple</item>
        <item name="colorPrimary">@color/appBar_purple</item>
        <item name="android:textColorPrimaryInverse">@color/backGround_Purple</item>
    </style>
</resources>
